# Diagrams

The canonical architecture image is [`architecture.png`](architecture.png); the root [`README.md`](../README.md) embeds it. A Mermaid version lives in a collapsible section there for text-only rendering.
